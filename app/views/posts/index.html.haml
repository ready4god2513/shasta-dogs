%div.post-header
    %h1 Shasta Dogs Blog
    - collection.each do |post|
        %h3= link_to post.title, post
        %small= "Posted #{time_ago_in_words(post.created_at)} ago by Holly"
        %div#content= simple_format(post.content).html_safe
        - if post != collection.last
            %hr